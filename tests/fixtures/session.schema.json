{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Session",
  "description": "Top-level session state.\n\nDesigned to be the unit of persistence: serialize this and you've captured enough to put the user back where they left off.\n\nFields are not `#[serde(default)]`: there are no live session files on disk to be backwards-compatible with, and silently defaulting in fields would defeat the schema-tripwire test that guards `CURRENT_SESSION_VERSION`.",
  "type": "object",
  "required": [
    "created_at",
    "id",
    "last_pid",
    "last_saved_at",
    "search_history",
    "sources",
    "streams",
    "tabs",
    "user_bookmarks",
    "version"
  ],
  "properties": {
    "created_at": {
      "description": "When the session was first created.",
      "type": "string",
      "format": "date-time"
    },
    "id": {
      "description": "Short id; also the filename stem on disk.",
      "allOf": [
        {
          "$ref": "#/definitions/SessionId"
        }
      ]
    },
    "last_pid": {
      "description": "PID of the seer process that most recently saved this session.  Recorded for diagnostics (e.g. a future concurrent-access warning); not consulted for correctness.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "last_saved_at": {
      "description": "When the saver last successfully wrote this session to disk.",
      "type": "string",
      "format": "date-time"
    },
    "search_history": {
      "description": "Recently submitted search patterns, most-recently-used first. Capped at [`MAX_SEARCH_HISTORY`].  Populated by successful search submissions in the TUI; used to drive the search prompt's history navigation (Up/Down).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "sources": {
      "description": "Sources this session was opened against.  Stored as an [`IdOrdMap`] keyed by id so the uniqueness invariant is encoded in the type, but serialized as a JSON array (same as [`Self::streams`]) — the `schemars(with = …)` annotation surfaces that shape to the schema generator, which has no `JsonSchema` impl for `IdOrdMap` of its own.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/SessionSource"
      }
    },
    "streams": {
      "description": "Log streams owned by this session, keyed by id.  Serialized as a JSON array because [`IdOrdMap`] writes itself that way; the explicit `schemars(with = …)` is what surfaces that to the schema generator since `IdOrdMap` has no `JsonSchema` impl of its own.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/LogStream"
      }
    },
    "tabs": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/Tab"
      }
    },
    "user_bookmarks": {
      "description": "Bookmarks indexed by the stream they target.  Inner buckets are [`IdOrdMap`]s so the per-stream uniqueness invariant is encoded in the type — `add_bookmark` becomes `insert_unique` and `remove_bookmark` an O(log n) lookup rather than a linear scan. Each inner bucket serializes as a JSON array via the `schemars(with = …)` shim, same trick as [`Self::streams`] and [`Self::sources`].",
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "$ref": "#/definitions/Bookmark"
        }
      }
    },
    "version": {
      "description": "On-disk schema version.  Bumped on changes that aren't otherwise serde-compatible (renames, restructured fields).",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    }
  },
  "definitions": {
    "Bookmark": {
      "description": "A user-created bookmark: a deliberate landmark in a log stream.\n\nA bookmark refers to its target by a byte-offset [`Cursor`] — feeding the cursor to [`crate::StreamView::seek_to_cursor`] lands the viewport on the bookmarked record.  This makes navigation `O(1)` (no walk from byte 0) and is stable across filter changes: a filter can hide the bookmarked event from view, but the cursor still names the same byte position regardless of what the active filter is.\n\n`display_time` and `display_msg` are captured at creation time so the Bookmarks tab can render the row even when the source isn't currently loaded — and so the preview reflects what the user saw when they made the bookmark, not whatever the file looks like now.",
      "type": "object",
      "required": [
        "created_at",
        "cursor",
        "display_msg",
        "display_name",
        "display_source",
        "display_time",
        "id"
      ],
      "properties": {
        "created_at": {
          "type": "string",
          "format": "date-time"
        },
        "cursor": {
          "type": "object",
          "additionalProperties": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0.0
          }
        },
        "display_msg": {
          "description": "First slice of the bookmarked event's `msg`, cached so the Bookmarks tab can show it without re-querying the engine.",
          "type": "string"
        },
        "display_name": {
          "description": "Bunyan `name` field of the bookmarked event (typically the component, e.g. `Nexus`), cached so the Bookmarks tab can show it without re-querying the engine.",
          "type": "string"
        },
        "display_source": {
          "description": "Source the bookmarked event came from, captured at creation time so the Bookmarks tab can show its filename without re-resolving the cursor against the engine.",
          "type": "string"
        },
        "display_time": {
          "description": "Timestamp of the bookmarked event, cached so the Bookmarks tab can show it without re-querying the engine.",
          "type": "string",
          "format": "date-time"
        },
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "name": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "CoreField": {
      "description": "One of the bunyan log record's structured top-level fields.\n\nEnumerated rather than carried as a string so that [`crate::source::SourceMetadata::excludes_all`] (and the per-record [`field_matches`] helper) can match exhaustively — adding a new core field is a compile error in those methods until threaded through.",
      "type": "string",
      "enum": [
        "Name",
        "Hostname",
        "Pid",
        "Msg"
      ]
    },
    "EventPredicate": {
      "description": "A predicate evaluated against an individual [`Event`].\n\nThe equality and regex variants carry a [`Form`] so that `name=Nexus` and `name!=Nexus` (and likewise `msg=~foo` / `msg!~foo`) are the same variant differing only in form.  `LevelAtLeast` has no useful negation (the user can write `level>=` at the threshold they want), so it is left as a single variant.",
      "oneOf": [
        {
          "description": "`event.level >= threshold`",
          "type": "object",
          "required": [
            "LevelAtLeast"
          ],
          "properties": {
            "LevelAtLeast": {
              "$ref": "#/definitions/Level"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "`event.level == level` (or `!=` when `form` is `Negated`)",
          "type": "object",
          "required": [
            "LevelEquals"
          ],
          "properties": {
            "LevelEquals": {
              "type": "object",
              "required": [
                "form",
                "level"
              ],
              "properties": {
                "form": {
                  "$ref": "#/definitions/Form"
                },
                "level": {
                  "$ref": "#/definitions/Level"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Named field has (or, when `form` is `Negated`, does not have) the given exact-string value.\n\n`name` is either a typed bunyan core field ([`FieldName::Core`]) or a free-form `extra` key ([`FieldName::Extra`]).",
          "type": "object",
          "required": [
            "FieldEquals"
          ],
          "properties": {
            "FieldEquals": {
              "type": "object",
              "required": [
                "form",
                "name",
                "value"
              ],
              "properties": {
                "form": {
                  "$ref": "#/definitions/Form"
                },
                "name": {
                  "$ref": "#/definitions/FieldName"
                },
                "value": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "`event.msg` matches (or, when `form` is `Negated`, does not match) the regex.",
          "type": "object",
          "required": [
            "MsgMatches"
          ],
          "properties": {
            "MsgMatches": {
              "type": "object",
              "required": [
                "form",
                "regex"
              ],
              "properties": {
                "form": {
                  "$ref": "#/definitions/Form"
                },
                "regex": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "`event.time` compared against `value` using `op`.  Negation is expressed by flipping the operator (e.g. `!(time >= X)` is `time < X`), so there is no separate [`Form`].",
          "type": "object",
          "required": [
            "TimeBound"
          ],
          "properties": {
            "TimeBound": {
              "type": "object",
              "required": [
                "op",
                "value"
              ],
              "properties": {
                "op": {
                  "$ref": "#/definitions/TimeOp"
                },
                "value": {
                  "type": "string",
                  "format": "date-time"
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "FieldName": {
      "description": "Name of a field a `field=value` predicate matches against.\n\nBunyan core fields are typed by [`CoreField`]; anything else lives in the event's `extra` map and is carried as the raw key string. The DSL parser converts a token's left-hand side via the `From<&str>` impl, which routes the known core names into [`Self::Core`] and everything else into [`Self::Extra`].\n\nSerializes / deserializes as a bare DSL spelling string (`\"name\"`, `\"hostname\"`, `\"build\"`, ...) via the `#[serde(into = ..., from = ...)]` round-trip, so the on-disk form mirrors what a user would type into the filter dialog.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "Core"
          ],
          "properties": {
            "Core": {
              "$ref": "#/definitions/CoreField"
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Extra"
          ],
          "properties": {
            "Extra": {
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "Filter": {
      "description": "A conjunction of predicates over an [`Event`].\n\nThe default value has no predicates and matches every event.",
      "type": "object",
      "required": [
        "predicates"
      ],
      "properties": {
        "predicates": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Predicate"
          }
        }
      }
    },
    "Form": {
      "description": "Whether a predicate is satisfied when its underlying condition holds (the affirmed form, e.g. `name=Nexus`) or when it doesn't (the negated form, e.g. `name!=Nexus`).  Replaces a bare `bool` so every match site has to enumerate both variants and the DSL parser no longer has `/* negated = */ false` comments at each call.",
      "oneOf": [
        {
          "description": "The predicate accepts events for which the condition is true (e.g. `name=Nexus`, `msg=~\"oh no\"`).",
          "type": "string",
          "enum": [
            "Affirmed"
          ]
        },
        {
          "description": "The predicate accepts events for which the condition is false (e.g. `name!=Nexus`, `msg!~\"oh no\"`).",
          "type": "string",
          "enum": [
            "Negated"
          ]
        }
      ]
    },
    "HostnameDisplay": {
      "description": "How [`format_event`] should render the bunyan `hostname` field.\n\nSelected via the `h` field-display dialog in the TUI and persisted on the host [`crate::stream::LogStream`] so the choice outlives a session.",
      "oneOf": [
        {
          "description": "Trim to the first dot-component, then collapse a trailing UUID to its first 8-character group — see [`short_hostname`].  This is the default because real Oxide hostnames carry a domain suffix and a service-instance UUID that crowd the header without informing the user.",
          "type": "string",
          "enum": [
            "short"
          ]
        },
        {
          "description": "Render the hostname exactly as it appears in the event.",
          "type": "string",
          "enum": [
            "full"
          ]
        },
        {
          "description": "Don't render the hostname column at all (no field, no surrounding spaces).",
          "type": "string",
          "enum": [
            "none"
          ]
        }
      ]
    },
    "Level": {
      "type": "integer",
      "enum": [
        10,
        20,
        30,
        40,
        50,
        60
      ]
    },
    "LogStream": {
      "description": "A log stream.\n\nOwns its identity, display name, and active filter.  The set of sources a stream draws from will join the struct once per-stream source restrictions land; for now every stream sees every source the engine knows about.\n\nFilter ownership lives here (rather than on the display tab) so that when a bookmark targets a stream that has no tab open, opening a fresh tab for that stream restores the user's filter alongside the position.",
      "type": "object",
      "required": [
        "id",
        "name"
      ],
      "properties": {
        "filter": {
          "default": {
            "predicates": []
          },
          "allOf": [
            {
              "$ref": "#/definitions/Filter"
            }
          ]
        },
        "hostname_display": {
          "description": "how the bunyan `hostname` field is rendered: short (the default), full, or omitted.  Set via the `h` field-display dialog in the TUI.",
          "default": "short",
          "allOf": [
            {
              "$ref": "#/definitions/HostnameDisplay"
            }
          ]
        },
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "name": {
          "type": "string"
        },
        "show_date": {
          "description": "when true, the leading timestamp on each rendered line carries its `YYYY-MM-DD` prefix; when false, the date is dropped and only the wall-clock part is shown.  Defaults to true: most triage spans more than a single day, and you want the date in view by default.  Toggled with `D` in the TUI.",
          "default": true,
          "type": "boolean"
        },
        "show_extras": {
          "description": "when true, render the structured fields beyond the bunyan header below each event; defaults to false because most triage starts from the header line and the user opts in (`F` in the TUI) when they need the details",
          "default": false,
          "type": "boolean"
        },
        "show_name": {
          "description": "when true, the bunyan `name` field renders in the header. Defaults to true: across merged sources the logger name is the quickest way to tell which component a given event came from.",
          "default": true,
          "type": "boolean"
        },
        "show_pid": {
          "description": "when true, the bunyan `pid` field renders alongside `name` in the header.  Defaults to false: real Oxide processes restart often enough that the literal pid number is rarely useful for triage; users opt in via the `h` field-display dialog when they do want it.",
          "default": false,
          "type": "boolean"
        },
        "show_raw": {
          "description": "when true, records render as the raw line bytes from the source rather than the formatted header/extras layout.  Useful for inspecting fields the parser dropped or normalized. Defaults to false; toggled with `R` in the TUI.",
          "default": false,
          "type": "boolean"
        }
      }
    },
    "Predicate": {
      "description": "A single predicate in a [`Filter`].\n\nSplit into two domains so that per-event matching and per-source matching are statically distinguishable.  Before the split, a single `Predicate::matches(event)` had a `SourceIdMatches` arm that returned `true` unconditionally — a silent no-op buried inside an otherwise honest method.  The wrapper keeps a single persisted list (so DSL ordering round-trips) while [`EventPredicate::matches`] and [`SourcePredicate::matches`] are each exhaustive over the variants they're called on.\n\nNew predicates land in [`EventPredicate`] if they read from an [`Event`], or in [`SourcePredicate`] if they read from a [`SourceId`].",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "Event"
          ],
          "properties": {
            "Event": {
              "$ref": "#/definitions/EventPredicate"
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Source"
          ],
          "properties": {
            "Source": {
              "$ref": "#/definitions/SourcePredicate"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "SessionId": {
      "type": "string",
      "maxLength": 8,
      "minLength": 8,
      "pattern": "^[0-9a-f]{8}$"
    },
    "SessionSource": {
      "description": "A source the session was opened against.\n\nCaptures the canonical path the user supplied plus a lightweight fingerprint (mtime + size) taken at open time.  At resume time, a mismatching fingerprint signals that the underlying file has changed and any cached parse state for it should be invalidated. The fingerprint is also the seed for any future path-independent matching (e.g. a re-extracted support tarball whose paths differ).",
      "type": "object",
      "required": [
        "id",
        "mtime",
        "path",
        "size"
      ],
      "properties": {
        "id": {
          "description": "id used to refer to this source from elsewhere in the session (cursors, bookmarks, log streams)",
          "type": "string"
        },
        "mtime": {
          "description": "file modification time captured at open time",
          "type": "string",
          "format": "date-time"
        },
        "path": {
          "description": "canonical path captured at open time",
          "type": "string"
        },
        "size": {
          "description": "file size in bytes captured at open time",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "SourcePredicate": {
      "description": "A predicate evaluated against a [`SourceId`] at source-selection time, not against any individual event.\n\nToday the only variant is regex-on-source-id, so the enum is trivially extensible.  Kept as an enum (rather than a plain struct) so future per-source predicates — matching on cached metadata, file mtime windows, etc. — slot in alongside without reshaping callers.",
      "oneOf": [
        {
          "description": "The source's id matches (or, when `form` is `Negated`, does not match) the regex.  The engine skips entire sources whose ids fail rather than iterating their contents.",
          "type": "object",
          "required": [
            "SourceIdMatches"
          ],
          "properties": {
            "SourceIdMatches": {
              "type": "object",
              "required": [
                "form",
                "regex"
              ],
              "properties": {
                "form": {
                  "$ref": "#/definitions/Form"
                },
                "regex": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "Tab": {
      "description": "A tab in the TUI.\n\nA tab is a view onto exactly one [`LogStream`] (referenced by id; the stream lives in [`Session::streams`]).  The tab's `name` is what the tab strip shows and what `--tab` matches against; it is independent of the backing stream's name so renaming a tab does not also rename the stream (and two tabs targeting the same stream can carry distinct names).  `cursor` is the byte-offset [`Cursor`] the tab is currently scrolled to, captured at save time so a resumed session can land the viewport on the same record.  `cursor` is `None` for an empty-or-unrendered tab.",
      "type": "object",
      "required": [
        "kind",
        "name",
        "stream"
      ],
      "properties": {
        "cursor": {
          "default": null,
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0.0
          }
        },
        "kind": {
          "$ref": "#/definitions/TabKind"
        },
        "name": {
          "type": "string"
        },
        "stream": {
          "type": "string",
          "format": "uuid"
        }
      }
    },
    "TabKind": {
      "description": "What a [`Tab`] displays.\n\nToday this is a binary distinction: regular log-record view versus the field/time histogram summary.  Persisted on each tab so a resumed session reopens both kinds in the right shape.",
      "oneOf": [
        {
          "description": "Regular per-record log view.",
          "type": "string",
          "enum": [
            "Stream"
          ]
        },
        {
          "description": "Histogram summary of the active filter's events.",
          "type": "string",
          "enum": [
            "Summary"
          ]
        }
      ]
    },
    "TimeOp": {
      "description": "Comparison operator for a [`EventPredicate::TimeBound`].",
      "oneOf": [
        {
          "description": "`event.time >= value`",
          "type": "string",
          "enum": [
            "AtLeast"
          ]
        },
        {
          "description": "`event.time > value`",
          "type": "string",
          "enum": [
            "After"
          ]
        },
        {
          "description": "`event.time <= value`",
          "type": "string",
          "enum": [
            "AtMost"
          ]
        },
        {
          "description": "`event.time < value`",
          "type": "string",
          "enum": [
            "Before"
          ]
        }
      ]
    }
  }
}
